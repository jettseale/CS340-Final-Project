<article id={{_id}} class="disease-card">
    <class="disease-card">
        <div class="disease-card-name">
            <p class="disease-card-name-p">{{diseaseName}}</p>
        </div>
        <div class="disease-card-id">
            <p class="disease-card-id-p">{{diseaseID}}</p>
        </div>
    </class>
</article>